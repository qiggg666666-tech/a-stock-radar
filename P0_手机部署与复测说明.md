# 当前仓库 P0 修复：手机部署与复测说明

本文件用于修复当前仓库中已经确认的两项阻断问题：**状态运行器未转发子脚本参数**，以及**工作流文件名末尾含不可见Unicode窄空格**。

## 必须覆盖的两个文件

| 文件 | 仓库位置 | 作用 |
| --- | --- | --- |
| `strategy_shard_runner.py` | 仓库根目录 | 向子脚本原样转发`--output-dir`、`--processes`、`--query-timeout`等参数，修复年线v5.2 A/B/C/D的退出码2。 |
| `a-stock-radar.yml` | `.github/workflows/a-stock-radar.yml` | 标准文件名的当前主Workflow，消除原文件末尾不可见窄空格带来的识别风险。 |

## 手机端部署顺序

1. 在网页代码档案中打开 **“最终·A-E 分片状态运行器”**，复制完整代码。
2. 打开GitHub仓库根目录已有的 `strategy_shard_runner.py`，点击 **Edit file**，全选旧代码并覆盖，文件名保持不变，直接提交到`main`。
3. 在网页代码档案中打开 **“最终生产 Workflow · 股票池 + A-E 五分片”**，复制完整代码。
4. 进入GitHub仓库的 `.github/workflows/`。若列表中已有名称看起来是 `a-stock-radar.yml` 的旧文件，请不要在原文件上依赖肉眼确认其末尾字符；使用 **Add file → Create new file**，新文件名手动输入：

   ```text
   a-stock-radar.yml
   ```

   然后粘贴Workflow完整内容并提交。
5. 回到 Actions 页面，确认出现标准的 `a-stock-radar.yml` 工作流。确认后再处理旧的带异常字符文件，避免中途没有可运行Workflow。

## 首次复测

1. 打开标准工作流，点击 **Run workflow**。
2. 手动目标仅选择 **`yearline-limitup-screener`** 或年线v5.2对应目标；不要选择`all`。
3. 观察 A/B/C/D 四片的“Run Year-Line Limit-Up Screener”步骤。

| 正确结果 | 异常结果 |
| --- | --- |
| 命令打印`STRATEGY_SHARD_COMMAND`，随后进入年线脚本扫描日志 | 仍显示`unrecognized arguments: --output-dir output --processes 2 --query-timeout 8` |
| A/B/C/D至少运行数秒并开始产生输出 | A/B/C/D均在0秒内退出码2 |

若仍出现旧错误，说明根目录的 `strategy_shard_runner.py` 尚未覆盖到修复版；请再次确认提交页面不是“Create new file”页面，而是已有文件的“Edit file”页面。

## 后续边界

本P0修复不会自动修复19个仍调用BaoStock `get_data()`的旧脚本。这些脚本要按批次改为游标逐行读取，避免pandas 2.x下的`DataFrame.append`错误；在年线v5.2复测通过前，不建议批量替换它们。
