# 首红与资本流：全市场直接运行（手机版）

> 这是按“直接接入、直接运行”要求准备的独立生产Workflow。它**不加入`all`**，不会启动旧策略；每次手动运行会准备一次共享股票池，并分别执行首红A-E五分片、资本流A-E五分片，最后只发一条汇总通知。

## 覆盖或新建这四个文件

| 类型 | GitHub路径 | 手机档案文件 |
| --- | --- | --- |
| 覆盖 | 仓库根目录`first_red_520.py` | `first_red_520.py`（全市场共享股票池+主动时限版） |
| 覆盖 | 仓库根目录`capital_flow_research_screener.py` | `capital_flow_research_screener.py`（主动时限版） |
| 新建 | 仓库根目录`research_signal_production_summary.py` | `research_signal_production_summary.py` |
| 新建 | `.github/workflows/research-signal-production.yml` | `research-signal-production.yml` |

`prepare_a_share_universe.py`应已在仓库根目录；如果不存在，请先从“最终生产文件”第06份复制它。不要改动原主Workflow，也不要选择其`all`。

## 手机上直接运行

1. 在GitHub完成四个文件的复制与 **Commit changes**，分支保持`main`。
2. 打开 **Actions**，等待出现新的 **Research Signal Production**。
3. 点开它，点击 **Run workflow**。
4. 分支选择`main`，直接点击绿色 **Run workflow**。

该Workflow无`target`、无`all`、无额外复选框。运行时会同时启动两类脚本各5片；每片有340分钟Runner上限，并在320分钟主动停止、写出checkpoint和状态JSON，避免硬超时完全丢失产物。

## 完成后只看一个结果

运行结束后，在Artifacts下载：

```text
research-signal-production-summary
```

打开其中的：

- `research_signal_production_summary.json`
- `research_signal_production_summary.md`

状态为`completed`表示可见的十个策略分片都写出了完成状态；`attention_required`表示至少一片出现缺失、主动时限、数据源或断路器问题。即使部分分片失败，也会有artifact和单条汇总通知；请把汇总文件或红色任务末尾日志发回，不要重跑`all`。

## 重要边界

首红的信号回测口径为信号后下一交易日开盘入场；资本流评分结合价格结构、量价代理和公开当日资金流快照，不识别账户级资金。全市场运行会消耗GitHub Actions并可能受公开数据源波动影响；筛选结果仅用于研究，不构成投资建议。
