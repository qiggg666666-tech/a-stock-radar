# 年线 v7 分片：Server酱与 Telegram 手机通知配置

本方案由 `yearline-v7-notify-summary` 汇总任务在四个分片完成后发送**一条**合并通知，避免 A/B/C/D 各自推送造成手机消息重复。通知任务即使失败，也不会改变四个扫描任务的执行结果或 artifact。

## 选择通知渠道

| 方案 | 到达方式 | 设置复杂度 | 推荐情形 |
| --- | --- | --- | --- |
| Server酱 Turbo | 绑定后的微信/已配置通道 | 低 | 希望最快完成设置、仅接收文本摘要 |
| Telegram Bot | Telegram 私聊、群组或频道 | 中 | 希望使用 Telegram、接收群组通知或后续扩展机器人功能 |
| 两者同时开启 | 两个渠道各收到同一条汇总 | 中 | 需要双通道备份 |

Server酱 Turbo 使用 SendKey 作为推送凭据。[1] Telegram Bot API 要求机器人令牌，并通过 HTTPS `sendMessage` 请求向指定 `chat_id` 发送消息。[2]

## 1. 上传两个新文件

| 本地文件 | 仓库路径 |
| --- | --- |
| `a-stock-radar-with-yearline-v7-notifications-final.yml` | `.github/workflows/a-stock-radar.yml`，完整覆盖主workflow |
| `yearline_v7_notify_summary.py` | 仓库根目录 |

同时保留此前已上传的 `yearline_limitup_v7_checkpoint.py`。

## 2. 配置 Server酱（可选）

访问 [Server酱 Turbo](https://sct.ftqq.com/)，完成账号与手机接收通道设置后获取 SendKey。随后在 GitHub 仓库进入：

```text
Settings → Secrets and variables → Actions → New repository secret
```

新建以下 Secret：

```text
Name:  SENDKEY
Secret: 粘贴您的Server酱SendKey
```

不要把 SendKey 写进 Python、YAML、README、Issues、网页代码档案或截图。

## 3. 配置 Telegram（可选）

在 Telegram 中打开 `@BotFather`，使用 `/newbot` 创建机器人并保存 token。先向新机器人发送 `/start`，再通过一个**本地可信终端**读取聊天ID：

```bash
export TELEGRAM_BOT_TOKEN='仅在本机临时设置，不要提交到GitHub'
curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates"
```

在返回 JSON 的 `message.chat.id` 中找到目标私聊或群组的 chat ID。群组必须先将机器人加入群组并发送一条消息，才会出现对应 update。Telegram 的机器人令牌是认证凭据；不要把它粘贴到聊天、网页、日志或仓库。

然后在 GitHub 仓库新增两个 Secret：

```text
Name:  TELEGRAM_BOT_TOKEN
Secret: BotFather生成的token

Name:  TELEGRAM_CHAT_ID
Secret: 上一步获得的message.chat.id
```

## 4. 运行和测试

部署完成后，在 GitHub Actions 手动选择：

```text
target = yearline-v7-checkpoint-a
yearline_v7_notify = true
```

扫描 job 会上传 A 分片 artifact，汇总通知 job 随后读取该 artifact 并发送一条包含候选数、checkpoint状态和 Top 候选的消息。工作日 16:30 自动运行时，四个分片都启动，汇总任务会在它们结束后发送一条合并消息。

| Secret 配置情况 | 预期行为 |
| --- | --- |
| 只配置 `SENDKEY` | 只发 Server酱通知 |
| 只配置 Telegram 两个 Secret | 只发 Telegram 通知 |
| 三个 Secret 都配置 | 两个渠道各发一条相同汇总 |
| 一个也未配置 | 扫描和 artifact 正常完成；汇总日志提示未配置，不会失败 |

手动运行时可将 `yearline_v7_notify` 改为 `false`，只生成 artifact 而不发送通知。定时运行默认启用通知。

## References

[1] [Server酱 Turbo 官网](https://sct.ftqq.com/)

[2] [Telegram Bot API 官方文档](https://core.telegram.org/bots/api)
