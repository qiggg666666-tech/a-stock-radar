# 自动运行与汇总通知：手机部署指南

本文件组按方案A为四条原本仅手动的独立研究任务增加工作日定时，并为两条原本自动但未推送的任务增加汇总后单条Server酱通知。所有股票池、分片、artifact与工作流仍完全独立；本次不修改任何研究公式、阈值、信号分层或候选排序。

| 顺序 | GitHub路径 | 目的 | 北京时间 |
|---:|---|---|---|
| 1 | `.github/workflows/a-share-transparent-research.yml` | A股透明研究自动运行 | 工作日17:15 |
| 2 | `.github/workflows/multi-factor-ohlcv-daily.yml` | Q01多因子自动运行 | 工作日18:15 |
| 3 | `.github/workflows/dinghai-multiperiod-research.yml` | 定海神针自动运行 | 工作日19:15 |
| 4 | `.github/workflows/research-signal-production.yml` | 研究信号生产自动运行 | 工作日21:30 |
| 5 | `adaptive_industry_rotation_summary.py` | 自适应行业轮动单条通知 | 保持工作日16:45 |
| 6 | `.github/workflows/adaptive-industry-rotation-full-market.yml` | 为自适应汇总注入通知参数与密钥 | 保持工作日16:45 |
| 7 | `lowrise_520_summary.py` | LowRise 520单条通知 | 保持工作日20:30 |
| 8 | `.github/workflows/lowrise-520-full-market.yml` | 为LowRise汇总注入通知参数与密钥 | 保持工作日20:30 |

## 覆盖方式

在手机GitHub中，每次只打开一个同名文件，选择 **Edit**，全选替换为对应完整文件后提交到 `main`。不要新建第二份同功能的workflow，也不要删除其他独立任务文件。所有工作流保留 `workflow_dispatch`，因此仍可在 Actions 页面手动复验。

## 消息推送边界

自适应行业轮动和LowRise 520仅在至少存在一个完成分片时，才从 GitHub Secrets 的 `SENDKEY` 尝试发送**一条**Server酱消息。未配置密钥、网络失败或Server酱返回业务失败码时，会将状态写入汇总JSON和Markdown，但不会阻断候选、错误或artifact上传；没有任何完成分片时写入 `skipped:no_completed_shards`，不会发送空摘要。

## 首次验收

提交后不必逐条立刻重跑；下一个对应时刻会自动触发。首次结果完成后，打开该任务最后的汇总artifact，确认JSON或Markdown中的 `notification` 为 `sent` 或明确的 `skipped`/`failed` 原因。`sent` 仅表示Server酱接口业务码为0，不代表具体手机接收通道一定已开启。
