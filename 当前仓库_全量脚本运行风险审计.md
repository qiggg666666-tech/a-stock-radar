# 当前仓库：全量脚本运行风险审计

**审计对象**：`qiggg666666-tech/a-stock-radar` 主分支提交 `5b12ee1`。  
**审计范围**：仓库根目录全部67份Python文件，以及当前工作流实际直接或通过状态运行器间接调用的52份脚本。  
**结论**：67份根目录脚本均能通过Python语法编译，但当前仓库存在两项会直接阻断部分或全部Actions运行的P0问题；另有19个当前工作流任务仍走BaoStock `get_data()`兼容风险路径。

> 本报告只评估代码可启动性、数据源兼容性和工作流契约；不代表任何策略表现或投资建议。

## 一、全量结果总览

| 审计项目 | 结果 |
| --- | ---: |
| 根目录Python脚本 | 67份 |
| 当前Workflow直接/间接调用脚本 | 52份 |
| Python语法编译失败 | 0份 |
| 直接调用且仍用BaoStock `get_data()` | 19份 |
| 非直接调用但仍用BaoStock `get_data()` | 11份 |
| 直接调用且存在无显式超时的`requests`调用 | 0份 |
| 当前确认的P0阻断项 | 2项 |

## 二、P0：必须先处理的阻断项

| 编号 | 当前证据 | 影响 | 最小修复 |
| --- | --- | --- | --- |
| P0-1 | 工作流文件真实名称是 `a-stock-radar.yml\u2009`，末尾含Unicode窄空格，而不是标准`.yml`后缀 | 新工作流识别、编辑和触发存在不确定性；手机端很难察觉该字符 | 创建一个**文件名完全为**`.github/workflows/a-stock-radar.yml`的新文件，再确认其显示在Actions中后删除带窄空格的旧文件 |
| P0-2 | 当前`strategy_shard_runner.py`只使用`parse_args()`，不会转发子脚本参数；Workflow有25处包装调用，年线v5.2 A-D明确传入了`--output-dir output --processes 2 --query-timeout 8` | 年线v5.2四片会像用户截图一样在启动前报“unrecognized arguments”，退出码2 | 覆盖为已提供的参数透传修复版运行器；该文件改用`parse_known_args()`并以参数列表安全启动子脚本 |

### 已被截图证实的失败链路

用户提供的年线v5.2 A/B/C/D截图与P0-2完全一致：四片均在0秒阶段失败，错误为`unrecognized arguments: --output-dir output --processes 2 --query-timeout 8`。这不是行情数据源、筛选条件或超时问题，而是**运行器版本与Workflow调用接口不匹配**。

## 三、P1：当前直接调用的BaoStock/pandas兼容风险

当前依赖包含`pandas>=2.2,<4.0`和`baostock`。已核验的BaoStock `ResultData.get_data()`内部依赖`DataFrame.append()`；pandas 2.x已移除该方法。因此下列19个当前工作流会调用的脚本，在落入BaoStock路径时可能失败。

| 风险脚本 | 备注 |
| --- | --- |
| `camarilla_screener.py` | 直接`get_data()` |
| `cox_sector_bot.py` | 有全局append兼容补丁，短期可运行但不建议继续依赖全局猴子补丁 |
| `dip_buy_screener.py` | 直接`get_data()` |
| `duck_head_wbottom_screener.py` | 直接`get_data()` |
| `dupont_roe_screener.py` | 直接`get_data()` |
| `first_red_macd_combo.py` | 直接`get_data()` |
| `first_red_sector_combo.py` | 直接`get_data()` |
| `index_support_signal.py` | 直接`get_data()` |
| `ma5_sideways.py` | 直接`get_data()` |
| `macd_resonance_screener.py` | 直接`get_data()` |
| `macd_zigzag_divergence.py` | 直接`get_data()` |
| `main.py` | 直接`get_data()` |
| `pipeline_sector_plus_signal.py` | 直接`get_data()` |
| `sector_flow_pcr_screener.py` | 直接`get_data()` |
| `sector_momentum_valuation.py` | 直接`get_data()` |
| `sector_rotation_alert.py` | 直接`get_data()` |
| `strong_continuation.py` | 有全局append兼容补丁，短期可运行但建议改为显式游标转换 |
| `vegas_tunnel_screener.py` | 直接`get_data()` |
| `wr_obv_screen.py` | 直接`get_data()` |

另有11份目前不由工作流调用的旧脚本也存在同一风险，但不应在当前故障期批量覆盖，避免修改未在运行的历史策略。

## 四、已确认正常或已具备兼容保护的关键链路

| 脚本/链路 | 静态结论 |
| --- | --- |
| `bottom_accumulation_screener_fast.py` | 编译通过，当前版本未使用BaoStock `get_data()` |
| `bull_confirm_screener_v4_fast.py` | 编译通过，当前版本未使用BaoStock `get_data()` |
| `pattern_breakout_screener.py` | 编译通过，当前版本未使用BaoStock `get_data()` |
| `prepare_a_share_universe.py` | 编译通过，当前版本未使用BaoStock `get_data()` |
| `dashboard_snapshot_publisher.py` | 编译通过，无BaoStock路径 |
| `strategy_shard_notify_summary.py` | 编译通过，无BaoStock路径 |
| `yearline_limitup_v7_checkpoint.py` | 编译通过，无BaoStock路径 |

上述“编译通过”不等于真实行情调用必然成功；它仅说明语法、导入结构与已识别的pandas兼容路径没有立即阻断。

## 五、建议的修复顺序

| 顺序 | 手机上要做的事 | 目的 | 验收标准 |
| --- | --- | --- | --- |
| 1 | 覆盖根目录`strategy_shard_runner.py`为参数透传修复版 | 解除年线v5.2四片启动前退出2 | 手动运行年线v5.2时A-D不再0秒失败 |
| 2 | 新建标准文件名`.github/workflows/a-stock-radar.yml`，内容复制当前工作流；确认Actions识别后再处理旧的带窄空格文件 | 消除不可见字符的工作流文件名风险 | Actions页显示新的标准名称工作流 |
| 3 | 只运行年线v5.2与底部吸筹做验证 | 先验证共享运行器与已修复分片链路 | A-D或A-E实际进入扫描步骤并产出artifact |
| 4 | 分两批改造上述19个仍用`get_data()`的直接调用脚本 | 消除pandas 2.x下的BaoStock失败风险 | 每批脚本通过编译与模拟游标转换测试 |
| 5 | 最后才评估11份未调用的历史脚本 | 避免当前修复扩大到未运行策略 | 不影响现行任务 |

## 六、审计边界

本次审计在公开仓库当前提交上完成；已包括全量编译、工作流调用清单、状态运行器参数接口、BaoStock `get_data()`、`DataFrame.append()`和显式`requests.timeout`静态检查。它不能替代真实市场时段下的接口可用性、AkShare限流、GitHub网络、Secrets配置和每个策略的业务正确性验证。

下一步最小修复应先完成P0-1和P0-2，而不是一次性修改19个数据源脚本。
